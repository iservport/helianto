package org.helianto.finance.domain;import static org.junit.Assert.assertEquals;import static org.junit.Assert.assertFalse;import static org.junit.Assert.assertTrue;import org.helianto.core.domain.Entity;import org.helianto.core.test.EntityTestSupport;import org.junit.Test;public class CostCentreTests {	@Test	public void equality() {		CostCentre costCentre = new CostCentre();		assertFalse(costCentre.equals(null));			CostCentre other = new CostCentre();		assertTrue(costCentre.equals(other));				Entity entity = EntityTestSupport.createEntity(1);			costCentre.setEntity(entity);		assertFalse(costCentre.equals(other));		costCentre.setCostCentreCode("01");		assertFalse(costCentre.equals(other));			other.setEntity(entity);		assertFalse(costCentre.equals(other));		other.setCostCentreCode("01");		assertTrue(costCentre.equals(other));		assertEquals(costCentre.hashCode(), other.hashCode());			other.setCostCentreCode("01.01");		assertFalse(costCentre.equals(other));    }}