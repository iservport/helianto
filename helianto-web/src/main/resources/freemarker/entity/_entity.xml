<?xml version="1.0" encoding="UTF-8"?>
<flow xmlns="http://www.springframework.org/schema/webflow"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/webflow
        http://www.springframework.org/schema/webflow/spring-webflow-2.0.xsd">
        
	<secured attributes="ROLE_ADMIN_MANAGER" />
   
	<persistence-context />

	<input name="entity"/>
	
	<on-start>
	    <set name="flowScope.basePath" value="'entity/'"/>
	    <set name="flowScope.assign"   value="'assign'"/>
	</on-start>

	<view-state id="edit.view" view="/frame2" model="entity">

		<on-render>
			<set name="viewScope.sidebar"  value="'entity_bar'" />
			<set name="viewScope.template" value="'entity_form'" />
		</on-render>

		<transition on="storeEntity" to="toCommit">
			<evaluate expression="entityAction.store(flowScope)" />
		</transition>
		
		<transition on="cancelForm" to="toReturn" bind="false"/>
		
	</view-state>

	<end-state id="toCommit" commit="true"/>

	<end-state id="toReturn" commit="true"/>


</flow>